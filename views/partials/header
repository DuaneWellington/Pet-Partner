<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <% title = typeof title !== 'undefined' ? title : "Pets for Adoption" %>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <nav>
      <!-- <a href="/pets/new" <%- title === 'Add Pet' ? 'class="active"' : '' %>>ADD PET</a>
      <a href="/pets" <%- title === 'All Pets' ? 'class="active"' : '' %>>>ALL PETS</a> -->
    </nav>
    <main>
      <h1><%= title %></h1>
    </main>
  </body>