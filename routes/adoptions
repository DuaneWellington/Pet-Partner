const express = require('express')
const router = express.Router();

const adoptionsCtrl = require('../controllers/adopters')

router.post('/pets/:id/adoptions', adoptionsCtrl.create)

module.exports = router